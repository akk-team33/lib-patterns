<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.team33.patterns</groupId>
        <artifactId>patterns-lib</artifactId>
        <version>2.9.0-SNAPSHOT</version>
    </parent>

    <artifactId>zz-typing-theta</artifactId>

    <name>Team33 Typing Library - Edition "theta"</name>
    <description>Provides representation and identification of definite types
        based on generic or non-generic classes.
    </description>

    <properties>
        <skipJavadoc>true</skipJavadoc>
        <skipHome>true</skipHome>
        <skipGPG>true</skipGPG>
    </properties>

    <dependencies>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>zz-lazy-lambda</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>arbitrary-mimas</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>enums-pan</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>records-triton</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
