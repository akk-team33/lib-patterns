<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.team33.patterns.annex</groupId>
        <artifactId>patterns-annex</artifactId>
        <version>2.9.0-SNAPSHOT</version>
    </parent>

    <artifactId>concept-alpha</artifactId>

    <name>Concept Alpha</name>
    <description>A collection of ideas, concepts and drafts</description>

    <dependencies>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>enums-pan</artifactId>
        </dependency>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>proving-kerberos</artifactId>
        </dependency>

        <!-- testing -->
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>arbitrary-mimas</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.team33.patterns</groupId>
            <artifactId>collection-ceres</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>